pipeline {
  agent { dockerfile true }
  stages {
    stage('Docker Test') {
      stages {
        sh  '''
          node --version
          git --version
          curl --version
      }  '''
    }
  }
}
